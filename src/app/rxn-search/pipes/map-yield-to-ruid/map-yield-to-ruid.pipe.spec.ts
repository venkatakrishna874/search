import { MapYieldToRuidPipe } from './map-yield-to-ruid.pipe';

describe('MapYieldToRuidPipe', () => {
  it('create an instance', () => {
    const pipe = new MapYieldToRuidPipe();
    expect(pipe).toBeTruthy();
  });
});
