import { MapToKeysPipe } from './map-to-keys.pipe';

describe('MapToKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new MapToKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
