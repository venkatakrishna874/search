import { TransformFilterTagPipe } from './transform-filter-tag.pipe';

describe('TransformFilterTagPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformFilterTagPipe();
    expect(pipe).toBeTruthy();
  });
});
